version: '3.8'

services:
  bot:
    build: .
    container_name: rusdi-bot
    env_file:
      - .env
    volumes:
      # Persist SQLite database
      - ./data:/app/data
    restart: unless-stopped
